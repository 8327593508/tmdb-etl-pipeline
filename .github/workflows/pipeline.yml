name: Daily TMDB ETL Pipeline

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"

jobs:
  etl:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Create .env
        run: |
          cat > .env << EOL
          TMDB_API_KEY=${{ secrets.TMDB_API_KEY }}
          PG_USER=${{ secrets.PG_USER }}
          PG_PASSWORD=${{ secrets.PG_PASSWORD }}
          PG_DB=${{ secrets.PG_DB }}
          PG_HOST=postgres
          PG_PORT=5432
          MAX_PAGES=${{ secrets.MAX_PAGES || 5 }}
          SCHEDULE=0
          EOL

      - name: Build images
        run: docker compose -f docker-compose.ci.yml build --no-cache

      - name: Start PostgreSQL
        run: docker compose -f docker-compose.ci.yml up -d postgres && sleep 12

      # 🔥 IMPORTANT: run ETL using `up`, not `run --rm`
      # This keeps the container alive so docker cp works
      - name: Run ETL
        run: docker compose -f docker-compose.ci.yml up app --exit-code-from app

      # 🔥 FIXED EXPORT SECTION (copy from running ETL container)
      - name: Export CSVs
        run: |
          mkdir -p data
          docker cp tmdb_pipeline_app:/app/data/popular_movies.csv data/popular_movies.csv || true
          docker cp tmdb_pipeline_app:/app/data/movie_details.csv data/movie_details.csv || true
          docker cp tmdb_pipeline_app:/app/data/movie_credits.csv data/movie_credits.csv || true

      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          file_pattern: "data/*.csv"
          commit_message: "chore: Update TMDB CSV exports [skip ci]"

      - name: Cleanup
        if: always()
        run: docker compose -f docker-compose.ci.yml down -v
